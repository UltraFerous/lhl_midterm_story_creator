<!DOCTYPE html>
<html lang="en">

<head>
  <title>Story Creator</title>

  <meta charset="UTF-8" >
  <meta http-equiv="X-UA-Compatible" content="IE=edge" >
  <meta name="viewport" content="width=device-width, initial-scale=1.0" >

  <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" />
  <link rel="stylesheet" href="/vendor/border-box.css" />
  <link rel="stylesheet" href="/styles/main.css" />
  <link rel="stylesheet" href="/styles/layout.css" />

  <script src="/vendor/jquery-3.0.0.js"></script>
  <!-- <script defer src="/scripts/index.js"></script> -->
</head>

<body>
  <%- include('partials/_nav'); %>
  <main>
    <section>
      <form action="/stories/new" method="GET">
        <button class="new-story-button" type="submit">Start a new story.</button>
      </form>
      <p>Collaborate with others to craft tales of swashbuckling adventure, fiery romance, or gut-busting comedy. The sky’s the limit.</p>
    </section>
    <section>
      <h1>Browse stories</h1>
      <!-- Programmatically insert story preview divs here -->
      <article class="story-list">
        <% for (let i = 0; i < storyData.length; i++) { %>
          <div class="story-overview">
            <a href="/stories/<%= storyData[i].id %>">
              <h1 class="story-overview-title"> <%= storyData[i].title %> </h1>
            </a>
            <h2 class="story-overview-author"> <%= storyData[i].name %> </h2>
            <h2 class="story-overview-preview"> <%= storyData[i].body %> </h2>
            <!-- Render contribute button if story status is Open -->
            <% if (storyData[i].status === "Open") { %>
              <a href="/stories/<%= storyData[i].id %>">
                <button>Contribute</button>
              </a>
            <% } %>
            <h3 class="story-overview-status">Status: <%= storyData[i].status ? "Open" : "Closed" %> </h3>
          </div>
       <% } %>
      </article>
    </section>
  </main>
</body>

</html>
